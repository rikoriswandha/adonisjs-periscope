---
'@rikology/adonisjs-periscope': patch
---

Decluttered the dashboard topbar. The theme selector, keyboard-shortcut help, pause toggle, and
clear action now live in a single options menu at the far right; a "Paused" chip appears next to
the page title only while recording is paused (click it to resume); and the application selector
renders only when the store contains more than one application. The header is now just the page
title, content search, optional application selector, and the options menu.
