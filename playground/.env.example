# Node
TZ=UTC
PORT=3333
HOST=localhost
NODE_ENV=development

# App
LOG_LEVEL=info
# Replace this placeholder with `node ace generate:key` before manual use.
APP_KEY=replace-with-a-generated-app-key
APP_URL=http://${HOST}:${PORT}

# Session
SESSION_DRIVER=cookie
