# Tests load this file after setting NODE_ENV=test, so they do not depend on a
# developer's ignored `.env`.
TZ=UTC
PORT=3333
HOST=127.0.0.1
NODE_ENV=test
LOG_LEVEL=info
APP_KEY=test-only-placeholder-app-key-01
APP_URL=http://${HOST}:${PORT}
SESSION_DRIVER=memory
